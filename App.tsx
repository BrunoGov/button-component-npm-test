import { View } from "react-native";
import { Button } from "./src/lib";

export default function App() {
  return <View style={{ marginTop: 200, padding: 10 }}>
    <Button text="Clique aqui!" />
  </View>;
}
